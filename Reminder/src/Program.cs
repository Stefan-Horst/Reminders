using System;

namespace Reminder.src
{
    class Program
    {
        public static ReminderManager reminderManager = new ReminderManager();
        //public static OutputTextWriter outputTextWriter = new OutputTextWriter(); //call this from remindermanager or commandexecuter (if even needed at all)

        static void Main(string[] args)
        {
            while (true)
            {
            //outputTextWriter.Welcome(1, "");


            }
        }
    }
}
